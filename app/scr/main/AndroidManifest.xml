<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="application.com.funagig">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- Location permissions for job location features -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Android 13+ media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JobPortalApp">

        <!-- Existing Activities -->
        <activity
            android:name=".InsertJobPostActivity"
            android:exported="false" />
        <activity
            android:name=".PostJobActivity"
            android:exported="false" />
        <activity
            android:name=".AllJobsActivity"
            android:exported="false" />
        <activity
            android:name=".RegistrationActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- New Activities -->
        <activity
            android:name=".activities.ServiceDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.UserProfileActivity"
            android:exported="false" />
        <activity
            android:name=".activities.EditProfileActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MyGigsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MyApplicationsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.BrighterMondayJobsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ApplicantsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AvailableGigsActivity"
            android:exported="false" />

        <!-- Main Launcher Activity -->
        <activity
            android:name=".HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Services -->
        <service
            android:name=".services.GigProcessingService"
            android:exported="false" />

        <!-- Broadcast Receivers -->
        <receiver
            android:name=".broadcasts.NetworkConnectivityReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".broadcasts.NotificationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="NEW_GIG_AVAILABLE" />
                <action android:name="GIG_APPLICATION_RECEIVED" />
                <action android:name="PAYMENT_RECEIVED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>